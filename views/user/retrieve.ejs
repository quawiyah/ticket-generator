<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Inconsolata:wght@200..900&display=swap" rel="stylesheet">
    <title>Retrieve Ticket</title>
    <style>
        body {
            min-height: 100vh;
            margin: 0;
            background: linear-gradient(135deg, #232526 0%, #414345 100%);
            font-family: "Inconsolata", monospace;
            color: #fff;
        }
        .main-container{
            min-height: 100vh;
            justify-content: center;
            display: flex;
            align-items: center;
        }
        .retrieve {
            width: 100%;
            max-width: 500px;
            padding: 40px 30px 30px 30px;
            background: rgba(30, 30, 40, 0.95);
            border-radius: 18px;
            box-shadow: 0 8px 32px 0 rgba(31, 38, 135, 0.37);
            text-align: center;
            position: relative;
        }
        .retrieve img:first-child {
            width: 160px;
            margin-bottom: 30px;
        }
        #greeting {
            font-size: 2.2rem;
            font-weight: 700;
            margin-bottom: 10px;
            color: #f87c5b;
            letter-spacing: 1px;
        }
        #details {
            font-size: 1.1rem;
            color: #e0e0e0;
            margin-bottom: 30px;
        }
        .ticket {
            background: rgba(255,255,255,0.06);
            border-radius: 16px;
            padding: 28px 24px 24px 24px;
            margin: 0 auto;
            width: 100%;
            max-width: 420px;
            box-shadow: 0 4px 24px 0 rgba(0,0,0,0.15);
            border: 1.5px solid #f87c5b33;
            position: relative;
            overflow: hidden;
        }
        .ticket:before {
            content: "";
            position: absolute;
            top: -60px;
            right: -60px;
            width: 120px;
            height: 120px;
            background: url("assets/images/pattern-ticket.svg") no-repeat center/cover;
            opacity: 0.12;
            z-index: 0;
        }
        .ticket > img {
            width: 90px;
            display: block;
            margin: 0 auto 10px auto;
        }
        .ticket > p {
            text-align: center;
            font-size: 1.05rem;
            color: #f87c5b;
            margin-bottom: 18px;
            font-weight: 600;
            letter-spacing: 0.5px;
        }
        .ticket-info {
            display: flex;
            align-items: center;
            gap: 1.5rem;
            z-index: 1;
            position: relative;
        }
        .ticket-info img {
            width: 80px;
            height: 80px;
            border-radius: 50%;
            border: 3px solid #f87c5b;
            object-fit: cover;
            box-shadow: 0 2px 10px 0 rgba(248,124,91,0.15);
            background: #fff;
        }
        .ticket-info > div:last-child {
            flex: 1;
        }
        #ticket-name {
            font-size: 1.15rem;
            font-weight: 600;
            color: #fff;
            margin-bottom: 6px;
        }
        #ticket-github {
            font-size: 0.98rem;
            color: #f87c5b;
            margin-bottom: 6px;
        }
        #ticket-name:last-child {
            font-size: 0.98rem;
            color: #e0e0e0;
            font-weight: 400;
        }
        @media (max-width: 600px) {
            .retrieve {
                padding: 18px 5vw 18px 5vw;
                max-width: 98vw;
            }
            .ticket {
                padding: 18px 8px 18px 8px;
                max-width: 98vw;
            }
            .ticket-info {
                flex-direction: column;
                gap: 1rem;
                align-items: flex-start;
            }
            .ticket-info img {
                margin-bottom: 8px;
            }
        }
    </style>
</head>
<body>
    <%- include('../partials/flash') %>
    <div class="main-container">
        <div class="retrieve">
            <h1 id="greeting">Congrats, <span style="color:#f87c5b"><%= information.full_name %></span>!<br>Your ticket is ready.</h1>
            <h5 id="details">We've emailed your ticket to <br> <span style="color:#f87c5b"><%= information.email %></span> and will send updates in <br> the run up to the event.</h5>
            <div class="ticket">
                <div class="ticket-info">
                <div>
                    <img src="<%= information.avatar %>" id="avatar-preview" alt="avatar" style="width:160px;height:160px;" />
                </div>
                <div>
                    <p id="ticket-name">Hello! <%= information.full_name %></p>
                    <p id="ticket-github">Your seat number is: <%= information.id %></p>
                </div>
                </div>
            </div>
        </div>
    </div>
    
</body>
</html>