<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Inconsolata:wght@400;700&display=swap" rel="stylesheet">
  <title>Register For Ticket</title>
  <style>
    :root {
      --primary: #ff4c60;
      --secondary: #8f5cf6;
      --bg-dark: #18122b;
      --bg-light: #232046;
      --input-bg: rgba(255,255,255,0.07);
      --border: #3a3456;
      --text-main: #fff;
      --text-muted: #bdb7d2;
      --accent: #ffe156;
    }
    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }
    body {
      min-height: 100vh;
      background: linear-gradient(135deg, var(--bg-dark) 60%, var(--secondary) 100%);
      color: var(--text-main);
      font-family: "Inconsolata", monospace;
      display: flex;
      align-items: center;
      justify-content: center;
    }
    .whole {
      background: var(--bg-light);
      border-radius: 24px;
      box-shadow: 0 8px 32px 0 rgba(31, 38, 135, 0.25);
      padding: 40px 32px 32px 32px;
      width: 100%;
      max-width: 420px;
      margin: 32px 0;
      display: flex;
      flex-direction: column;
      align-items: center;
      position: relative;
      overflow: hidden;
    }
    .whole::before {
      content: "";
      position: absolute;
      top: -60px; left: -60px;
      width: 180px; height: 180px;
      background: radial-gradient(circle, var(--secondary) 0%, transparent 70%);
      opacity: 0.15;
      z-index: 0;
    }
    .whole img {
      width: 120px;
      margin-bottom: 18px;
      z-index: 1;
    }
    h1 {
      font-size: 2.1rem;
      font-weight: 700;
      margin-bottom: 10px;
      letter-spacing: 0.5px;
      z-index: 1;
    }
    h5 {
      font-size: 1.1rem;
      color: var(--text-muted);
      margin-bottom: 28px;
      font-weight: 400;
      z-index: 1;
    }
    form {
      width: 100%;
      z-index: 1;
    }
    label {
      display: block;
      font-size: 1rem;
      font-weight: 700;
      margin-bottom: 6px;
      color: var(--accent);
      letter-spacing: 0.5px;
    }
    input[type="text"],
    input[type="email"] {
      width: 100%;
      padding: 12px 14px;
      margin-bottom: 18px;
      border: 1.5px solid var(--border);
      border-radius: 8px;
      background: var(--input-bg);
      color: var(--text-main);
      font-size: 1rem;
      transition: border 0.2s;
    }
    input[type="text"]:focus,
    input[type="email"]:focus {
      outline: none;
      border-color: var(--secondary);
      background: rgba(255,255,255,0.12);
    }
    button {
      width: 100%;
      padding: 13px 0;
      border: none;
      border-radius: 8px;
      background: linear-gradient(90deg, var(--primary) 60%, var(--secondary) 100%);
      color: var(--bg-dark);
      font-size: 1.1rem;
      font-weight: 700;
      letter-spacing: 1px;
      cursor: pointer;
      margin-top: 10px;
      box-shadow: 0 2px 8px 0 rgba(255,76,96,0.10);
      transition: background 0.2s, transform 0.1s;
    }
    button:hover {
      background: linear-gradient(90deg, var(--secondary) 60%, var(--primary) 100%);
      transform: translateY(-2px) scale(1.02);
    }
    .upload-wrapper {
      width: 100%;
      margin-bottom: 22px;
    }
    .upload-wrapper label {
      color: var(--accent);
      font-size: 1rem;
      font-weight: 700;
      margin-bottom: 8px;
    }
    .upload-box {
      position: relative;
      border: 2px dashed var(--border);
      border-radius: 12px;
      padding: 32px 10px 18px 10px;
      text-align: center;
      background: var(--input-bg);
      cursor: pointer;
      transition: border-color 0.3s;
      margin-bottom: 8px;
    }
    .upload-box:hover {
      border-color: var(--secondary);
      background: rgba(143,92,246,0.07);
    }
    .upload-box input[type="file"] {
      position: absolute;
      top: 0; left: 0;
      width: 100%;
      height: 100%;
      opacity: 0;
      cursor: pointer;
    }
    .upload-content img {
      width: 38px;
      height: 38px;
      margin-bottom: 8px;
      opacity: 0.85;
    }
    .upload-content p {
      color: var(--text-muted);
      font-size: 0.98rem;
    }
    .upload-note {
      margin-top: 2px;
      font-size: 0.85rem;
      color: #a9a4c2;
      text-align: left;
    }
    @media (max-width: 600px) {
      .whole {
        padding: 24px 8px 18px 8px;
        max-width: 98vw;
      }
      h1 {
        font-size: 1.3rem;
      }
      .whole img {
        width: 80px;
      }
    }
  </style>
</head>
<body>
  <a href="/user/login" style="position: absolute; top: 18px; left: 18px; background: none; border: none; color: var(--accent); font-weight: 700; font-size: 1.5rem; text-decoration: underline; z-index: 2; margin: 0; padding: 0; cursor: pointer;">
      Admin
    </a>
  <div class="whole">
    <h1>Your Journey to Coding Conf 2025 Starts Here!</h1>
    <h5>Secure your spot at next year's biggest coding conference.</h5>
    <form id="register-form" action="/user/store" method="post" enctype="multipart/form-data">
    <div class="upload-wrapper">
      <label for="avatar">Upload Avatar</label>
      <div class="upload-box">
        <input type="file" id="avatar" name="avatar" accept="image/png, image/jpeg" />
        <div class="upload-content">
          <p>Drag and drop or click to upload</p>
        </div>
        <!-- This will show the confirmation -->
        <p id="uploadStatus" style="color: lightgreen; font-weight: bold; display: none;">Image uploaded successfully ✅</p>
      </div>
      <p class="upload-note">Upload your photo (JPG or PNG, max size: 500KB).</p>
      <div class="invalid-feedback">
        <%= locals?.errors?.avatar ? locals.errors.avatar.msg : '' %>
      </div>
    </div>
    <div>
      <label for="name">Full Name</label>
      <input type="text" 
        class="form-control <%= errors?.full_name ? 'is-invalid' : '' %>" id="name" 
        name="full_name" value="<%= old?.full_name || '' %>" required>
      <div class="invalid-feedback">
        <%= errors?.full_name?.msg %>
      </div>
    </div>
    <div>
      <label for="email">Email Address</label>
      <input type="email" 
      class="form-control <%= errors?.email ? 'is-invalid' : '' %>" id="email" 
      name="email" value="<%= old?.email || '' %>" placeholder="example@email.com" required>
      <div class="invalid-feedback">
        <%= errors?.email?.msg %>
      </div>
    </div>
    <div>
      <label for="phone">Phone Number</label>
      <input type="text" 
      class="form-control <%= errors?.phone ? 'is-invalid' : '' %>" id="phone" 
      name="phone" value="<%= old?.phone || '' %>" placeholder="phone_number" required>
      <div class="invalid-feedback">
        <%= errors?.phone?.msg %>
      </div>
    </div>
    <button type="submit" id="btn">Generate My Ticket</button>
    </form>
  </div>

  <% if (flash && flash.message) { %>
  <div class="position-fixed top-0 start-50 translate-middle-x mt-3 z-3" style="width: 90%; max-width: 500px;">
    <div class="alert alert-<%= flash.type === 'error' ? 'danger' : 'success' %> alert-dismissible fade show shadow-lg" role="alert">
      <%= flash.message %>
      <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    </div>
  </div>

  <script>
    setTimeout(() => {
      const alert = document.querySelector('.alert');
      if (alert) {
        alert.classList.remove('show');
        alert.classList.add('fade');
        alert.style.opacity = '0';
      }
    }, 5000);
  </script>
  <% } %>

  <script>
    const avatarInput = document.getElementById('avatar');
    const uploadStatus = document.getElementById('uploadStatus');

    avatarInput.addEventListener('change', function () {
      const file = this.files[0];
      if (file) {
        const allowedTypes = ['image/jpeg', 'image/png'];
        if (!allowedTypes.includes(file.type)) {
          uploadStatus.style.display = 'none';
          alert('Only JPG or PNG files are allowed');
          this.value = ''; // clear the file input
          return;
        }

        if (file.size > 500 * 1024) {
          uploadStatus.style.display = 'none';
          alert('File must be less than 500KB');
          this.value = ''; // clear the file input
          return;
        }

        uploadStatus.style.display = 'block';
      } else {
        uploadStatus.style.display = 'none';
      }
    });
  </script>

  <%- include('../partials/flash') %>
</body>
</html>
